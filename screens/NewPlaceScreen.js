import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const NewPlaceScren = (props) => {
  return (
    <View>
      <Text>NewPlaceScren</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default NewPlaceScren;
